# 🧱 Framework principal
Django==5.2.6                        # Framework web base del proyecto
djangorestframework==3.16.1         # API REST para Django
django-filter==25.2                 # Filtros en vistas API (por categoría, etc.)

# 🔐 Seguridad
djangorestframework_simplejwt==5.5.1 # Autenticación JWT
PyJWT==2.10.1                        # Manejo de tokens JWT

# 🌐 Consumo de APIs
requests==2.32.5                    # Peticiones HTTP para consumir NewsAPI

# 🗃️ Conexión con Oracle
cx_Oracle==8.3.0                    # Conector clásico para Oracle
# oracledb==3.3.0                   # Alternativa moderna (no usada en este proyecto)

# 🖼️ Visualización y utilidades
pillow==11.3.0                      # Manejo de imágenes (si se usa en frontend)
sqlparse==0.5.3                     # Formateo de SQL en Django

# 🌍 Compatibilidad y entorno
asgiref==3.9.1                      # Soporte para ASGI (Django internamente)
certifi==2025.8.3                   # Certificados SSL
charset-normalizer==3.4.3          # Codificación de caracteres
cryptography==46.0.1               # Seguridad y cifrado
cffi==2.0.0                         # Interfaz para C (usado por cryptography)
pycparser==2.23                     # Parser para C (usado por cffi)
idna==3.10                          # Codificación internacional de dominios
urllib3==2.5.0                      # Manejo de conexiones HTTP

# ⚙️ Herramientas de entorno (no necesarias en producción)
pipenv==2025.0.4                    # Gestión de entornos virtuales (si se usa)
virtualenv==20.34.0                 # Creación de entornos virtuales
setuptools==80.9.0                  # Instalación de paquetes
wheel==0.45.1                       # Empaquetado de proyectos
distlib==0.4.0                      # Librería interna de virtualenv
platformdirs==4.4.0                 # Manejo de rutas por sistema operativo
tzdata==2025.2                     # Datos de zona horaria
